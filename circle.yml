version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          yarn: true
          executor: cypress/browsers-chrome69
          browser: chrome
          parallel: true
          parallelism: 2
          post-steps:
          - store_artifacts:
              path: cypress/reports
