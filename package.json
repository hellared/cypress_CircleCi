{
  "devDependencies": {
    "@testing-library/cypress": "^6.0.0",
    "cypress": "^4.3.0"
  },
  "scripts": {
    "test": "NODE_OPTIONS=--max-http-header-size=1000000 cypress run --headless --browser chrome --record --key"
  }
}